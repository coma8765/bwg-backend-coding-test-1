"""Service with chatbot receives the message and sends it to a chat(*)

This is a full-fledged program (for use, see `src.__main__`).

The program offers users to send a reply message with data(**).
After receiving the program forwards the data(**) to the chat(*)


*specified chat see `.env.example` (TELEGRAM_CHAT_ID)
**data - a full name and an image
"""
